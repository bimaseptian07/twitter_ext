# Shopee remote via extension

Untuk membantu grab secara distribusi dengan websocket, dengan http api

Support grab

1. masih http `GET` di url `/fetch` dengan payload:
```
{
    uri: "https://shopee.co.id/get_item"
}
```

